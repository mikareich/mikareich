---
type Props = {
  tags: string[];
};

const { tags } = Astro.props;
---

<div class="flex gap-4">
  {
    tags.map((tag) => (
      <span class="tag small m-0 truncate">
        <span class="text-blue-200">#</span>
        {tag}
      </span>
    ))
  }
</div>
